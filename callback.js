const vedio = {
    title : "a",
    tags:["a","b","c","d"],
    showTag() {
        // return this.tags.forEach(function(tag) {
                console.log(vedio.tags[2])
        // });
    }
}
vedio.showTag();
